---
import Link from '../../components/ui/Link/Link.astro';
import type { FooterLink } from '../../types/link';

type Props = {
  links: FooterLink[];
};
---

<nav class="flex w-full justify-center gap-4 flex-wrap">
  {
    Astro.props.links.map(link => (
      <Link href={link.href} hoverLink>
        {link.label}
      </Link>
    ))
  }
</nav>
