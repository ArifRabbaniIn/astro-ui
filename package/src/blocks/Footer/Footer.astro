---
import Footer1 from './Footer1.astro';
import type { FooterProps } from './types';

type Props = FooterProps;

const { variation } = Astro.props;
---

{variation === '1' ? <Footer1 {...Astro.props} /> : null}
