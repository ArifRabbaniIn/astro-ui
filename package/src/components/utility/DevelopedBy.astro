---
import type { ComponentProps } from 'astro/types';
import HeartIcon from '../icons/HeartIcon.astro';
import Link from '../ui/Link/Link.astro';

type Props = ComponentProps<typeof Link> & {
  authorName: string;
  authorWebsiteUrl: string;
};

const { authorName, authorWebsiteUrl, ...restProps } = Astro.props;
---

<Link
  target="_blank"
  href={authorWebsiteUrl}
  class:list={['no-underline', Astro.props.class]}
  {...restProps}>
  Crafted with <HeartIcon dimOnHover={false} fill class="inline" /> by {
    authorName
  }
</Link>
